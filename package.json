{
  "name": "organizador-financeiro-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Organizador Financeiro - Frontend deployment",
  "scripts": {
    "dev": "cd FRONTEND && npm run dev",
    "build": "cd FRONTEND && npm run build",
    "preview": "cd FRONTEND && npm run preview"
  },
  "engines": {
    "node": ">=18"
  }
}
