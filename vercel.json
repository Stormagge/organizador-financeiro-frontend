{
  "buildCommand": "node setup.js && cd FRONTEND && npm install && npm run build",
  "outputDirectory": "FRONTEND/dist",
  "installCommand": "npm install",
  "github": {
    "enabled": true,
    "silent": true
  }
}
